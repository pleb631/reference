# AGENTS.md

本文件约束本仓库中的文档新增与修改。除非任务另有明确要求，编辑 Markdown 时应遵循以下规则。

## 仓库定位

本仓库是面向开发者的中文快速参考（cheatsheet）集合。文档的目标是让读者快速找到命令、语法、参数或常见示例，而不是提供从原理到实践的长篇教程。

- `docs/*.md`：各主题的备忘清单，构建后成为独立页面。
- `README.md`：网站首页及文档导航；新增主题时通常需要同步添加入口。
- `docs/quickreference.md`：本仓库 Markdown 语法、卡片样式和布局能力的权威参考。
- `CONTRIBUTING.md`：贡献流程、首页导航和本地开发说明。
- `scripts/assets/`：首页卡片图标；图标文件名应与 `docs` 中的 Markdown 文件名一致。

修改前先阅读同类文档；涉及特殊布局时，再查阅 `docs/quickreference.md`，优先复用已有写法和类名。

## 写作风格

- 默认使用简体中文，产品名、命令、API、选项和代码标识符保留官方拼写。
- 以“快速查阅”为第一目标：短句、短段落、明确标题，先给可复制的示例，再补充必要说明。
- 内容保持实用和客观，避免寒暄、营销式表达、空泛总结及不必要的背景铺垫。
- 一个卡片只讲一个紧密相关的主题；内容过长时拆分为多个 `###` 卡片。
- 说明应回答“它做什么、怎么用、关键参数是什么”。不要在示例中省略会影响正确执行的必要步骤。
- 示例应最小、完整、可理解。占位符使用清晰名称，如 `<branch>`、`<file>`、`<port>`，并保持同一文档内一致。
- 不臆造命令、参数、输出或兼容性信息。版本相关内容应注明适用版本；无法确认的内容不要写成确定事实。
- 中文与英文、数字、行内代码之间通常留一个空格；中文标点使用全角，代码和命令中的符号保持原样。
- 避免重复：同一知识点已有文档时使用相对链接，并只补充当前上下文必需的信息。

## 文档结构

主题页通常采用以下结构：

````markdown
主题名称 备忘清单
===

一句话说明本页覆盖的内容和用途。

入门
---

### 常用操作

```语言
可复制的最小示例
```

必要的补充说明。

### 参数说明

参数 | 说明
--- | ---
`--example` | 简洁说明
<!--rehype:className=style-list-->

另见
---

- [官方文档](https://example.com/) _(example.com)_
````

结构约定：

- 页面标题使用 Setext 一级标题：标题下一行写 `===`。
- 页面标题后紧跟一段简短介绍，说明主题与页面范围。
- 大分类使用 Setext 二级标题：标题下一行写 `---`。
- 每个 `###` 三级标题会成为一个独立卡片，是正文的主要组织单位。
- `####` 仅用于卡片内部的小节，不要用它替代新的独立卡片。
- 相关的官方资料和仓库内链接放在文末“另见”部分；链接文字应描述目标内容，避免使用“点击这里”。
- 不要仅为视觉效果增加无内容的标题或层级。

## Markdown 语法约定

### 代码与命令

- 命令、路径、文件名、参数、环境变量和代码标识符使用反引号包裹。
- 多行示例使用围栏代码块，并尽量标注准确语言，如 `bash`、`shell`、`json`、`yaml`、`javascript`。
- 命令块保持可复制；只有在确实需要表达终端提示符时才加入 `$`。
- 注释放在需要说明的命令附近，避免把大段解释塞进代码块。
- 超长且不适合横向滚动的代码块，可在代码块后添加：

  ```html
  <!--rehype:className=wrap-text-->
  ```

### 表格与列表

- 参数、选项、键值对和简短对照信息优先使用表格。
- 步骤、条件或资料集合使用列表；条目保持平行句式。
- 表格单元格应简洁。内容需要长篇解释时，改用小节而不是制造超宽表格。
- 快捷键表格可使用 `shortcuts` 或 `shortcuts-last`；普通说明表格可使用 `style-list`、`style-list-arrow`、`show-header`、`auto-wrap`、`left-align` 等已有类。

### 链接、图片和强调

- 仓库内链接使用相对路径，例如 `[Markdown](./markdown.md)`。
- 外部技术事实尽量链接到官方文档；“另见”中的外链可在末尾标注域名，如 `_(example.com)_`。
- 图片必须提供有意义的替代文本，不使用无信息量的 `image` 或 `截图`。
- 粗体只用于真正需要强调的关键词；不要用粗体代替标题。
- 警告、注意和技巧可以使用 GitHub 风格提示块，但只在信息确实重要时使用。

## Rehype 布局注释

本仓库使用紧随 Markdown 元素之后的 HTML 注释控制页面布局和样式：

```html
<!--rehype:key=value&key=value-->
```

使用原则：

- 注释紧跟它所作用的标题、段落、表格或代码块，不在两者之间插入无关内容。
- 优先复用 `docs/quickreference.md` 中已展示的类和格式，不自行发明类名。
- 卡片跨列或跨行分别使用 `col-span-*`、`row-span-*`，例如：

  ```html
  <!--rehype:wrap-class=col-span-2 row-span-2-->
  ```

- 页面卡片列数可通过二级标题后的 `body-class=cols-*` 调整；除非内容确有需要，不要覆盖默认布局。
- 样式应服务于可读性。不要仅为装饰添加内联颜色、尺寸或复杂布局。
- 修改已有页面时，保留不属于当前任务的布局注释；删除或更改前先确认其渲染用途。

具体支持的语法、类名及效果以 `docs/quickreference.md` 为准。

## 首页导航与新文档

新增 `docs/<name>.md` 时：

1. 使用小写、清晰、稳定的文件名；多词名称通常用连字符连接。
2. 在 `README.md` 的合适分类中添加相对链接，沿用相邻卡片的格式与配色写法。
3. 如需首页图标，在 `scripts/assets/` 添加同名 SVG，例如 `docs/cron.md` 对应 `scripts/assets/cron.svg`。
4. SVG 应使用 `height="1em"`、`width="1em"`，颜色优先使用 `currentColor`。
5. 检查新增文档与已有主题是否重复，并补充必要的双向“另见”链接。

仅修改已有文档内容时，不要无理由调整首页导航、图标或其他页面。

## 编辑原则

- 保持变更聚焦，不顺手重写无关内容，也不要批量改变现有排版风格。
- 保持文件为 UTF-8、空格缩进、缩进宽度 2，并在文件末尾保留换行。
- 尊重现有文档中的中英文术语、命令风格和卡片顺序；修复明显错误时不要改变原意。
- 不直接编辑构建产物；源文档应在 `README.md`、`docs/` 或相应资源目录中修改。
- 若任务要求与本规范冲突，以任务的明确要求为准，并尽量保持仓库其余约定。

## 校验

完成修改后，根据变更范围执行：

```bash
npm run markdownlint
npm run build
```

- `npm run markdownlint` 带有 `--fix`，可能修改多个 `docs/*.md`；运行后必须检查 `git diff`，不得提交与任务无关的自动格式化变更。
- 至少确认标题层级、围栏代码块、表格、相对链接和 `rehype` 注释均闭合且位置正确。
- 构建失败时先修复源 Markdown，不要手工修补生成的 HTML。
